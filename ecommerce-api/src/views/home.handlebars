{{!-- Vista estática --}}
<h1>Productos (render estático)</h1>

<ul>
  {{#each products}}
    <li>
      <strong>{{this.title}}</strong> — ${{this.price}}<br />
      {{this.description}}
    </li>
  {{/each}}
</ul>

<a href="/realtimeproducts">Ir a productos en tiempo real</a>